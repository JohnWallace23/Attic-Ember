# OS / system files
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
.DS_Store

# OneDrive
*.tmp
~$*

# Editor / IDE
.vscode/
.idea/

# Environment / secrets
.env
.env.local
*.local

# Jekyll build output & cache (generated locally, never committed)
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Bundler lockfile — regenerated locally; kept out of git on purpose
# (a Windows-pinned lock broke the Cloudflare build once)
Gemfile.lock

# Dependencies / build output (uncomment as needed)
# node_modules/
# dist/
# build/
